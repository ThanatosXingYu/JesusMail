name: jesusmail

services:
    pgsql-jesusmail:
      image: postgres:17.4-alpine
      hostname: pgsql
      volumes:
        - ./postgresql-data:/var/lib/postgresql/data
        - ./postgresql-socket:/var/run/postgresql
      environment:
        - TZ=${TZ}
        - POSTGRES_DB=${DBNAME}
        - POSTGRES_USER=${DBUSER}
        - POSTGRES_PASSWORD=${DBPASS}
      restart: always
      ports:
        - "${SQL_PORT:-127.0.0.1:25432}:5432"
      networks:
        jesusmail-network:
          aliases:
            - pgsql

    redis-jesusmail:
      image: redis:7.4.2-alpine
      hostname: redis
      entrypoint: ["/bin/sh","/redis-conf.sh"]
      volumes:
        - ./redis-data:/data
        - ./conf/redis/redis-conf.sh:/redis-conf.sh
      restart: always
      ports:
        - "${REDIS_PORT:-127.0.0.1:26379}:6379"
      environment:
        - TZ=${TZ}
        - REDISPASS=${REDISPASS}
      sysctls:
        - net.core.somaxconn=4096
      networks:
        jesusmail-network:
          aliases:
            - redis

    rspamd-jesusmail:
      image: jesusmail/rspamd:1.2
      build:
        context: ./Dockerfiles/rspamd
        dockerfile: Dockerfile
      hostname: rspamd
      depends_on:
        - redis-jesusmail
      environment:
        - TZ=${TZ}
        - REDISPASS=${REDISPASS}
        - RETENTION_DAYS=${RETENTION_DAYS:-7}
      volumes:
        - ./conf/rspamd/local.d:/etc/rspamd/local.d
        - ./conf/rspamd/statistic.conf:/etc/rspamd/statistic.conf
        - ./conf/rspamd/rspamd.conf:/etc/rspamd/rspamd.conf 
        - ./rspamd-data:/var/lib/rspamd
        - ./logs/rspamd:/var/log/rspamd
      restart: always
      networks:
        jesusmail-network:
          aliases:
            - rspamd

    dovecot-jesusmail:
      image: jesusmail/dovecot:1.6
      build:
        context: ./Dockerfiles/dovecot
        dockerfile: Dockerfile
      hostname: dovecot
      depends_on:
        - pgsql-jesusmail
        - redis-jesusmail
      cap_add:
        - NET_BIND_SERVICE
      volumes:
        - ./conf/dovecot/conf.d:/etc/dovecot/conf.d
        - ./conf/dovecot/dovecot.conf:/etc/dovecot/dovecot.conf
        - ./conf/dovecot/rsyslog.conf:/etc/rsyslog.conf
        - ./logs/dovecot:/var/log/mail
        - ./ssl:/etc/ssl/mail
        - ./ssl-self-signed:/etc/ssl/ssl-self-signed
        - ./vmail-data:/var/vmail
        - ./rspamd-data:/var/lib/rspamd
        - ./postgresql-socket:/var/run/postgresql
      environment:
        - DBNAME=${DBNAME}
        - DBUSER=${DBUSER}
        - DBPASS=${DBPASS}
        - TZ=${TZ}
        - JESUSMAIL_HOSTNAME=${JESUSMAIL_HOSTNAME}
        - REDISPASS=${REDISPASS}
        - RETENTION_DAYS=${RETENTION_DAYS:-7}
      ports:
        - "${IMAP_PORT:-143}:143"
        - "${IMAPS_PORT:-993}:993"
        - "${POP_PORT:-110}:110"
        - "${POPS_PORT:-995}:995"
      restart: always
      ulimits:
        nproc: 65535
        nofile:
          soft: 20000
          hard: 40000
      networks:
        jesusmail-network:
          aliases:
            - dovecot

    postfix-jesusmail:
      image: jesusmail/postfix:1.6
      build:
        context: ./Dockerfiles/postfix
        dockerfile: Dockerfile
      hostname: postfix
      depends_on:
        pgsql-jesusmail:
          condition: service_started
      volumes:
        - ./conf/postfix/main.cf:/etc/postfix/main.cf
        - ./conf/postfix/master.cf:/etc/postfix/master.cf
        - ./conf/postfix/conf:/etc/postfix/conf
        - ./conf/postfix/sql:/etc/postfix/sql
        - ./conf/postfix/rsyslog.conf:/etc/rsyslog.conf
        - ./logs/postfix:/var/log/mail
        - ./ssl:/etc/ssl/mail
        - ./postfix-data:/var/spool/postfix
        - ./rspamd-data:/var/lib/rspamd
        - ./postgresql-socket:/var/run/postgresql
      environment:
        - TZ=${TZ}
        - DBNAME=${DBNAME}
        - DBUSER=${DBUSER}
        - DBPASS=${DBPASS}
        - REDISPASS=${REDISPASS}
        - JESUSMAIL_HOSTNAME=${JESUSMAIL_HOSTNAME}
        - RETENTION_DAYS=${RETENTION_DAYS:-7}
      cap_add:
        - NET_BIND_SERVICE
      ports:
        - "${SMTP_PORT:-25}:25"
        - "${SMTPS_PORT:-465}:465"
        - "${SUBMISSION_PORT:-587}:587"
      restart: always
      networks:
        jesusmail-network:
          aliases:
            - postfix


    webmail-jesusmail:
      image: roundcube/roundcubemail:1.6.11-fpm-alpine
      hostname: roundcube
      depends_on:
        - pgsql-jesusmail
        - dovecot-jesusmail
        - postfix-jesusmail
      volumes:
        - ./webmail-data:/var/www/html
        - ./conf/webmail/mime.types:/var/roundcube/config/mime.types
        - ./conf/webmail:/var/roundcube/config
        - ./conf/webmail/plugins/jesusmail_sso:/var/www/html/plugins/jesusmail_sso:ro
        - ./conf/php:/usr/local/etc
        - ./php-sock/:/var/run/
      environment:
        - TZ=${TZ}
        - ROUNDCUBEMAIL_DB_TYPE=pgsql
        - ROUNDCUBEMAIL_DB_HOST=pgsql
        - ROUNDCUBEMAIL_DB_NAME=${DBNAME}
        - ROUNDCUBEMAIL_DB_USER=${DBUSER}
        - ROUNDCUBEMAIL_DB_PASSWORD=${DBPASS}
        - ROUNDCUBEMAIL_DEFAULT_HOST=dovecot
        - ROUNDCUBEMAIL_DEFAULT_PORT=${IMAP_PORT:-143}
        - ROUNDCUBEMAIL_SMTP_SERVER=postfix
        - ROUNDCUBEMAIL_SMTP_PORT=${SMTP_PORT:-25}
        - ROUNDCUBEMAIL_REQUEST_PATH=/roundcube
        - JESUSMAIL_ROUNDCUBE_SSO_SECRET=${JESUSMAIL_ROUNDCUBE_SSO_SECRET:-}
        - JESSUSMAIL_ROUNDCUBE_SSO_SECRET=${JESSUSMAIL_ROUNDCUBE_SSO_SECRET:-}
        - JESUSMAIL_ROUNDCUBE_SSO_CORE_URL=${JESUSMAIL_ROUNDCUBE_SSO_CORE_URL:-http://core/api/public/mailbox/login_ticket/consume}
      restart: always
      networks:
        jesusmail-network:
          aliases:
            - webmail

    core-jesusmail:
      image: jesusmail/core:4.9.3
      build:
        context: .
        dockerfile: Dockerfiles/core/Dockerfile
      hostname: core-manage
      depends_on:
        - pgsql-jesusmail
      volumes:
        - ./ssl:/etc/ssl/mail
        - ./ssl-self-signed:/etc/ssl/ssl-self-signed
        - ./conf/core/fail2ban/filter.d:/etc/fail2ban/filter.d
        - ./conf/core/fail2ban/jail.d:/etc/fail2ban/jail.d
        - ./logs/fail2ban:/var/log/fail2ban
        - ./postgresql-socket:/opt/jesusmail/postgresql-socket
        - ./php-sock:/opt/jesusmail/php-sock
        - ./rspamd-data:/opt/jesusmail/rspamd-data
        - ./webmail-data:/opt/jesusmail/webmail-data
        - ./.env:/opt/jesusmail/.env
        - ./conf:/opt/jesusmail/conf
        - ./logs:/opt/jesusmail/logs
        - ./logs/core:/opt/jesusmail/core/logs
        - ./core-data:/opt/jesusmail/core/data
        - /var/run/docker.sock:/var/run/docker.sock:ro
        - ./vmail-data:/opt/jesusmail/vmail-data
      environment:
        - TZ=${TZ}
        - FAIL2BAN_INIT=${FAIL2BAN_INIT:-y}
        - JESUSMAIL_ROUNDCUBE_SSO_SECRET=${JESUSMAIL_ROUNDCUBE_SSO_SECRET:-}
        - JESSUSMAIL_ROUNDCUBE_SSO_SECRET=${JESSUSMAIL_ROUNDCUBE_SSO_SECRET:-}
      cap_add:
        - NET_BIND_SERVICE
        - NET_ADMIN
        - NET_RAW
      ports:
        - "${HTTP_PORT:-80}:${HTTP_PORT:-80}"
        - "${HTTPS_PORT:-443}:${HTTPS_PORT:-443}"
      restart: always
      networks:
        jesusmail-network:
          aliases:
            - core


networks:
  jesusmail-network:
    driver: bridge
    driver_opts:
      com.docker.network.bridge.name: br-jesusmail
    ipam:
      driver: default
      config:
        - subnet: ${IPV4_NETWORK:-172.66.1}.0/24

